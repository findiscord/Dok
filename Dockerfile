# Используйте базовый образ с поддержкой вашего приложения. Например, Ubuntu.
FROM ubuntu:22

# Устанавливаем любые необходимые зависимости. В данном случае нет зависимостей.

# Определяем, какой порт будет слушать контейнер.
EXPOSE 8080

# Команда, которая будет выполняться при запуске контейнера.
CMD ["echo", "WORK"]
